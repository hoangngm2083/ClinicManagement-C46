FROM pgvector/pgvector:pg15

# Use the base pgvector image which already has the extension installed
# This image is based on postgres:15 and includes pgvector extension

# Copy initialization scripts if needed
# COPY init-pgvector.sql /docker-entrypoint-initdb.d/

# The pgvector extension will be available automatically

